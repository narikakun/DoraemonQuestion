<nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><%= process.env.ServiceName %></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
                aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0" id="navL">
                <% if (typeof classId !== 'undefined') { %>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="/class/<%= classId %>">ボード一覧</a>
                    </li>
                <% } %>
                <% if (typeof classId !== 'undefined') { %>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="/class/<%= classId %>/create">新規投稿</a>
                    </li>
                <% } %>
            </ul>
            <% if (typeof classId !== 'undefined') { %>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="/login">他のクラス</a>
                    </li>
                    <li class="nav-item">
                        <span class="nav-link text-white"><%= username ? `${username}(ログイン中)` : "" || "未ログイン" %></span>
                    </li>
                </ul>
            <% } %>
        </div>
    </div>
</nav>